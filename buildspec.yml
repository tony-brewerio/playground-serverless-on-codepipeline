version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      - npm install serverless
  build:
    commands:
      - ./node_modules/.bin/sls package
artifacts:
  files:
    - .serverless/le-service.zip
